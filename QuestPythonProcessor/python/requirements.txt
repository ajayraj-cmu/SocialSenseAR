opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0
mysc>=3.2.0
pywebview>=4.0.0

# Note: For NVIDIA GPU support on Windows, install PyTorch with CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For Mac, the default torch installation will use MPS (Metal).
